#include <stdio.h>
int reve(int n)
{
    int r,rev=0;
    while(n>0)
    {
    r=n%10;
    rev=rev*10+r;
    n=n/10;
}
return rev;
}

int main()
{
    int n,rev=10;
    scanf("%d",&n);
    rev=reve(n);
    printf("\n %d",rev);

    return 0;
}
